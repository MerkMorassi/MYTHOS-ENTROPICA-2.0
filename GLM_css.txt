/* Z:\MYTHOS-VAULT-SERVER\css\style.css */
/* MYTHOS VAULT // CANONICAL STYLE v1.2 (Codebase Restoration) */

:root {
    --bg-color: #0a0a0a;            /* Deep Black/Dark Gray */
    --panel-bg: #111111;            /* Slightly lighter panel background */
    --main-color: #00ffaa;          /* The Canonical Mythos Green */
    --text-color: #cccccc;          /* Light gray for general text */
    --dim-text: #666666;            /* Dim gray for labels and system messages */
    --error-color: #ff3333;
    --border-color: #00ffaa;        /* Green border on panels */
    --font-mono: 'Menlo', 'Consolas', 'Courier New', monospace;
}

* { box-sizing: border-box; }

body {
    background-color: var(--bg-color);
    color: var(--text-color);
    font-family: var(--font-mono);
    margin: 0;
    padding: 20px;
    font-size: 13px;
    height: 100vh;
    display: flex;
    flex-direction: column;
    overflow: hidden;
}

/* --- TYPOGRAPHY & LAYOUT --- */
h1, h2, h3 { 
    margin: 0 0 10px 0; 
    text-transform: uppercase; 
    letter-spacing: 1px;
    font-weight: normal;
}
h2 { 
    color: var(--main-color); 
    border-bottom: 1px solid var(--border-color); 
    padding-bottom: 5px; 
    font-size: 1.1em;
}

.layout {
    display: grid;
    grid-template-columns: 300px 1fr; 
    gap: 20px;
    flex-grow: 1;
    overflow: hidden;
    margin-top: 10px;
    height: 95%; 
}

.col {
    display: flex;
    flex-direction: column;
    gap: 20px;
    overflow-y: auto;
    padding-right: 5px;
}

/* --- PANELS --- */
.panel {
    background: var(--panel-bg);
    border: 1px solid var(--border-color); 
    padding: 15px;
    display: flex;
    flex-direction: column;
    gap: 10px;
}
.col:last-child > .panel { flex-grow: 1; }

.panel-header {
    border-bottom: 1px solid #222;
    padding-bottom: 5px;
    margin-bottom: 5px;
    color: var(--main-color);
    font-weight: bold;
    display: flex;
    justify-content: space-between;
}

/* --- INPUTS --- */
.input-group { display: flex; flex-direction: column; gap: 4px; }
label { font-size: 0.75em; color: var(--dim-text); text-transform: uppercase; }

input, select, textarea {
    width: 100%;
    background: #000;
    border: 1px solid var(--dim-text);
    color: var(--text-color);
    padding: 8px;
    font-family: var(--font-mono);
    font-size: 1em;
    transition: border-color 0.2s;
}
input:focus, textarea:focus { outline: none; border-color: var(--main-color); }
textarea { resize: none; min-height: 40px; }

/* --- BUTTONS --- */
button {
    background: var(--main-color); 
    color: #000;
    border: none;
    padding: 8px 12px;
    cursor: pointer;
    font-family: var(--font-mono);
    text-transform: uppercase;
    font-size: 0.75em;
    transition: 0.2s;
    font-weight: bold;
}
button:hover { background: #00e090; }

/* --- CHAT / LOGS (ChatPack) --- */
#messages-container, #chatHistory {
    flex-grow: 1;
    background: #000;
    padding: 15px;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 10px;
    font-size: 0.9em;
    border: 1px solid #1a1a1a;
}

.msg { 
    max-width: 95%; 
    padding: 8px 12px; 
    border-radius: 0;
    line-height: 1.4; 
    word-wrap: break-word;
    margin: 5px 0;
}
.msg.user { 
    align-self: flex-end; 
    text-align: right; 
    color: var(--text-color); 
    background: #181818; 
    border-right: 2px solid var(--main-color); 
}
.msg.bot { 
    align-self: flex-start; 
    color: var(--text-color); 
    background: #000;
    border-left: 2px solid var(--main-color); 
}
.msg.sys { color: var(--dim-text); font-style: italic; align-self: flex-start; }
.msg.err { color: var(--error-color); border: 1px solid var(--error-color); background: #1a0000; }
.ok { color: var(--main-color); }

/* Query Area */
#queryArea {
    display: flex;
    gap: 10px;
    padding-top: 10px;
    border-top: 1px solid #222;
}
#queryArea textarea { flex-grow: 1; }
#send-button { width: 120px; background: var(--main-color); color: #000; border: none; }

/* Misc Elements */
#activeAgentDisplay { color: var(--main-color); }
#memoryNodesDisplay { color: var(--text-color); } 
#keyStatus { color: var(--error-color); }
#keyStatus.ok { color: var(--main-color); }

/* --- CODE-SHOP CONSOLE STYLES (Final Layout Stabilization) --- */
.code-shop-layout {
    display: flex; 
    flex-direction: column; 
    flex-grow: 1; 
    gap: 20px;
    height: 100%; 
}

#code-input-area {
    flex-shrink: 0; 
    display: flex;
    flex-direction: column;
}

/* Ensures output area grows to fill the remaining height in the flex column */
.code-shop-layout > .panel:last-child {
    flex-grow: 1;
}

#codeSnippetInput {
    flex-grow: 1;
    font-family: var(--font-mono);
    font-size: 0.9em;
    resize: none;
}

#analysis-output {
    flex-grow: 1;
    background: #000;
    border: 1px solid var(--main-color);
    padding: 15px;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
}

.analysis-card {
    background: var(--panel-bg);
    border: 1px solid #333;
    padding: 15px;
    margin-bottom: 15px;
}

.analysis-card h3 {
    color: var(--main-color);
    border-bottom: 1px dashed #333;
    padding-bottom: 5px;
    margin-bottom: 10px;
}

/* CRITICAL SCROLL FIX FOR CODE BLOCKS */
.analysis-card pre {
    background: #000;
    border: 1px solid #222;
    padding: 10px;
    white-space: pre-wrap;
    word-wrap: break-word;
    font-size: 0.85em;
    color: #ccc;
    max-height: 400px; /* Limits height */
    overflow-y: auto;  /* Enables scrolling */
}

.status-tag { margin-top: 10px; }
.status-tag.ok { color: var(--main-color); }
.status-tag.err { color: var(--error-color); }

/* --- ADDED COMPONENTS FROM PREVIOUS EXAMPLE --- */

/* Progress Bar Component */
.progress-bar {
    width: 100%;
    height: 8px;
    background-color: var(--panel-bg);
    border-radius: 4px;
    margin-top: 10px;
    overflow: hidden;
}

.progress-fill {
    height: 100%;
    background-color: var(--main-color);
    width: 0%;
    transition: width 0.3s ease;
}

/* List Item Component (for manifest lists) */
.list-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 8px 0;
    border-bottom: 1px solid #222;
}

.list-item:last-child {
    border-bottom: none;
}

.list-item .name {
    color: var(--main-color);
}

.list-item .id {
    color: var(--dim-text);
    font-size: 0.9em;
}